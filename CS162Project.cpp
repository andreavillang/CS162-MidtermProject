#include <iostream>

using namespace std;

//schedule algorithm for first come, first serve
int fcfs(int arr [])
{
	int index, arrival, burst, priority;
	int counter = 0;
	while(counter < burst)
	{
		
	}
	return 0;
}

int main()
{
    int numTestCase, numBlocks, numProcess;
	string schedule;
	
	cin >> numTestCase;
	cin >> numProcess >> schedule;
	
	int arrInt [3][numBlocks];
	
	/*
	for(int i = 0; i < numTestCase; i++)
	{
		schedule.toUpperCase();
		if(schedule == "FCFS")
		{
			for(int j = 0; j < numBlocks; j++)
			{
				//put fcfs algorithm codes here.
			}
		}
	}
	*/
	cout << numTestCase << endl << numProcess << endl << schedule << endl;

    return 0;
}